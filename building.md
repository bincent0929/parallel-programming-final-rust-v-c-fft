For `c-fft/main.c`
`make THREAD_COUNT=n`
`n` being an integer value.

`cargo build -p rust-fft`
`THREAD_COUNT=n cargo build -p rust-fft`
`n` being an integer value.

`cargo build -p spectrum-analyzer`

or use `benchmark.sh`!
